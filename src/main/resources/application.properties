# ===================================================================
# Datasource Configuration
# ===================================================================
spring.datasource.url=jdbc:mysql://localhost:3306/user_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===================================================================
# JPA / Hibernate Configuration
# ===================================================================
spring.jpa.hibernate.ddl-auto=none

# ===================================================================
# JWT Configuration
# ===================================================================
# IMPORTANT: Use a strong, base64-encoded secret in a real environment. Generate one from a site like 'https://www.allkeysgenerator.com/Random/Security-Encryption-Key-Generator.aspx'
jwt.secret.key=c29tZXRoaW5nX3Zlcnlfc2VjdXJlX2FuZF9sb25nX2tleV9mb3Jfand0X3NlY3JldA==
# 1 hour in milliseconds
jwt.expiration.ms=3600000
# 7 days in milliseconds
jwt.refresh-token.expiration.ms=604800000

# ===================================================================
# Default Admin User Configuration
# ===================================================================
app.default-admin.username=admin
app.default-admin.password=adminpassword